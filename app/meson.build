app_src = [
    'src/main.cpp'
]

app_inc = include_directories(['include'])

app = executable('app',
    dependencies: horus_dep,
    sources: app_src,
    include_directories: app_inc,
    install: true)
